user source-t
# source-t's write permission removed. source-t remains a valid,
# authenticated user (password_file unchanged) -- this is a scoping
# change to an existing enforcement mechanism (protocol A10), not a
# redesign, and not a connection-level denial.

user source-c
topic write test/topic

user receiver
topic read test/topic
